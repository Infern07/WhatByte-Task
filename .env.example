# Copy this file to `.env` in the project root.
# Firebase Console → Project settings → Your apps → Web app → apiKey
#
# No spaces around `=`. Restart dev server after changes:
#   npx expo start -c

EXPO_PUBLIC_API_KEY=
